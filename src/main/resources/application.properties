# Server
spring.application.name=ai-proxy-backend

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Perfil activo (se sobreescribe en Render)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

